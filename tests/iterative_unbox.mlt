let rec box_n_values n boxed =
  if n = 0 then boxed else box_n_values (n - 1) ((box 3 n*n as v in v) :: boxed)

run let boxed = box_n_values 5 [] in delay 10
    fold_left (fun acc value -> unbox 3 value as v in acc + v) 0 boxed
